spring:
  application:
    name: "Homes-merry"

  datasource:
    url: "jdbc:mysql://localhost:3306/Homes_Merry"
    username: "root"
    password: "root@root"
    driver-class-name: "com.mysql.cj.jdbc.Driver"

  jpa:
    hibernate:
      ddl-auto: "update"
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: "org.hibernate.dialect.MySQLDialect"

  data:
    web:
      pageable:
        default-page-size: 20
        max-page-size: 100
        one-indexed-parameters: false

JWT_SECRET: "12345xcvsfsdgsvahfbvkjsbvhsvkjsdvkhs"

springdoc:
  default-flat-param-object: true
  swagger-ui:
    operations-sorter: method